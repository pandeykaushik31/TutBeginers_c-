// Use logical operator to find whether the age of a person lies between 10 and 20
const age = 15;

const isAgeBetween10And20 = age >= 10 && age <= 20;


console.log(`Is the age between 10 and 20? ${isAgeBetween10And20}`);
